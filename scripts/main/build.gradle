// In the asset pack's build.gradle file:
plugins {
  id 'com.android.asset-pack'
}

assetPack {
    packName = "main" // Directory name for the asset pack
    dynamicDelivery {
        deliveryType = "install-time"
    }
}
